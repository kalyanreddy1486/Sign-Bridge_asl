# ----------------------------------------------------------------------
# ASL Two-Way Bridge — Python dependencies
# Pin ranges are intentionally loose but anchored below the next major
# that is known to break MediaPipe / TensorFlow compatibility.
# ----------------------------------------------------------------------

# Core: vision + inference
opencv-python>=4.8.0,<5.0
mediapipe>=0.10.0,<0.11
tensorflow>=2.13.0,<2.17
numpy>=1.24.0,<2.0
Pillow>=10.0.0

# Training / evaluation (only needed if you retrain from data/)
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Web server: Flask + Socket.IO for real-time streaming
flask>=2.3.0,<4.0
flask-socketio>=5.3.0
flask-cors>=4.0.0

# Production WSGI server (used in Dockerfile / Procfile)
eventlet>=0.35.0
